package com.londono.distribuciones.common.domain;

/**
 * Roles de usuario. En el alcance inicial solo existe ADMIN; el enum queda
 * preparado para crecer sin reescribir el modelo.
 */
public enum Role {
    ADMIN
}
